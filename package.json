{
  "name": "sqlite-workbench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone browser SQL workbench for a SQLite file. Read-only by default; opt-in editing with guard rails.",
  "license": "MIT",
  "bin": {
    "sqlite-workbench": "bin/cli.js"
  },
  "scripts": {
    "start": "bun run bin/cli.js",
    "dev": "bun --hot bin/cli.js",
    "test": "bun test test/"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "lit": "^3.3.3",
    "sql-formatter": "^15.8.2"
  }
}

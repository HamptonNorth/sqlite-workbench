{
  "//": "Project registry for the support scripts (scripts/remote.js). Copy this to",
  "//1": "projects.json (git-ignored) and edit. See docs/OPERATIONS.md for the full guide.",
  "//2": "Resolution order: $SWB_PROJECTS, then ./projects.json, then ~/.config/sqlite-workbench/projects.json.",

  "projects": {
    "myapp": {
      "host": "user@server.example.com",
      "remoteDb": "/srv/myapp/db/app.sqlite",
      "stop": "sudo systemctl stop myapp",
      "start": "sudo systemctl start myapp",
      "port": 9999
    },
    "billing": {
      "host": "deploy@billing.example.com",
      "remoteDb": "/opt/billing/data.sqlite",
      "stop": "sudo systemctl stop billing",
      "start": "sudo systemctl start billing",
      "port": 9998,
      "//sudo": "Set sudo:true when the DB is owned by a service account (e.g. under /opt or /var/lib) and your ssh user can read but not overwrite it. The file ops (backup/replace) then run via sudo, preserving the file's owner/permissions.",
      "sudo": true
    }
  }
}
